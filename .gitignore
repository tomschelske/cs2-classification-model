# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.ipynb_checkpoints/

# Data & model artifacts (large / regenerable — do not commit)
*.dem
*.rar
data/demos/**
data/*.parquet
data/**/*.parquet
data/parse_report.json
models/*.pkl
models/*.joblib
# ...but keep metrics.json (headline numbers) and the tiny deployed model.pkl
# (2.6KB linear kernel) so the Docker build / CI can copy it into the image.
!models/metrics.json
!models/model.pkl

# Frontend
frontend/node_modules/
frontend/dist/

# OS / editor
.DS_Store
.idea/
.vscode/
