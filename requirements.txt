# Core pipeline
awpy>=2.0            # demo utilities; requires Python >= 3.11 (we parse via demoparser2 directly)
demoparser2==0.41.2  # PIN: 0.41.4 has a global-state bug — the 2nd parse call per
                     # process panics (garbage alloc) on py3.13/arm64. 0.41.2 is clean.
polars               # awpy returns Polars DataFrames
pandas               # .to_pandas() interop + sklearn convenience
pyarrow              # Parquet read/write

# Modeling
scikit-learn         # baseline, logistic regression, calibration, metrics
lightgbm             # gradient-boosted classifier
shap                 # Phase 5 interpretation

# Serving
fastapi              # POST /predict
uvicorn[standard]    # ASGI server
pydantic             # request/response schemas

# Dev / exploration
jupyter              # Phase 1 schema exploration
matplotlib           # calibration curves, SHAP plots
tqdm                 # progress bars for the corpus parse
